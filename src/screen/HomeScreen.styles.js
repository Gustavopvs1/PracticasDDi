import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
        marginTop: 40,
        color: '#79B547',
        fontSize: 30,
        textAlign: 'center',
        fontWeight: 'bold',
        marginBottom: 20,
    },
    backGroundImage: {
        flex: 1,
    },
    spiner: {
        marginTop: 10,
        marginBottom: 50,
    }
})